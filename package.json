{
  "name": "web-fig",
  "version": "2.0.0",
  "description": "Suporte Estudo_Car no WhatsApp (Meta Cloud API)",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start",
    "test": "cd backend && npm test"
  }
}
